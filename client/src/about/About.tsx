const About = () => {

    return (
        <p>
            This app will allow the user to manage resources that travel to a processing plant. From there the plant will process and send products to different locations based on a schedule.
        </p>
    );
};

export default About;